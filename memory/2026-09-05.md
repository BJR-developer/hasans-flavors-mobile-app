# Memory 2026-09-05

- Configured Supabase backend for Hasan's Flavors using project `hasan's flavors` (`wplbfxyudyndgzkucbia`). <!-- project: github.com/BJR-developer/hasans-flavors-mobile-app -->
- Implemented 3 distinct user roles:
  1. `owner`: Has exclusive access to executive owner analytics and metrics (`/analytics` on web, `/staff/owner` on mobile). Strictly restricted from Cashier POS and Kitchen KDS.
  2. `cashier`: Manages the POS Register (`/pos`), Kitchen KDS (`/kds`), Orders table (`/orders`), and Menu/Stock manager (`/inventory`). Restricted from Owner financials.
  3. `customer`: Accesses the full customer mobile app and dining menu.
- Migrated and seeded database tables in Supabase:
  - `profiles`: 3 demo accounts (`owner@hasan.com`, `cashier@hasan.com`, `customer@hasan.com`).
  - `categories`: 8 restaurant categories.
  - `dishes`: 67 authentic dishes with prices, images, ratings, and stock status.
  - `dining_tables`: 12 dining tables with real-time occupancy status.
  - `orders`: 19 multi-day orders with complete items, modifier options, and revenue totals.
- Added Supabase Realtime pub/sub subscriptions to both Web operations app and React Native / Expo mobile app.
- Added AsyncStorage caching for onboarding (`@hasan_onboarded_v2`), splash view (`@hasan_splash_seen_v2`), and user profiles in the mobile app.
- Created `.env.example` in both repositories with documented connection keys.
